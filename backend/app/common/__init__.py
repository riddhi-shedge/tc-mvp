"""Shared infrastructure used by more than one part (auth verification).

Not deal data and not business logic — parts still exchange deal state only
through validated Payloads (rules/architecture.md)."""
