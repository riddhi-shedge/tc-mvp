# Frontend env — copy to frontend/.env (gitignored). ANON KEY ONLY here:
# the frontend must never see the service-role key (rules/code-style.md).
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key
VITE_API_BASE=http://localhost:8000
